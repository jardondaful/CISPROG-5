bit_string = input("Enter a string of bits: ")
last_letter = bit_string[1:]
rest_of_bit_string = bit_string[0]
print("The bit string '" + last_letter + rest_of_bit_string + "' is the bit string you inputted but each bit of it is shifted left by 1")
